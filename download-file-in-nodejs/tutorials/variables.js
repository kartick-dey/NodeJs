var city = 'kolkata';

{
    let city = 'Bankura';
    console.log(city);
}

console.log(city);

const arr = [1, 2, 3];
// arr = [1, 2] --- can'nt reasign in.
arr.push(4);
console.log(arr)

/**
 * Most common programming case type
 * 1. camelCase
 * 2. kabab-case
 * 3. snake_case
 * 4. PascalCase
 */

 /**
  * Data type in JS
  * 1. Primitive data type - memory allocate in stack - (string, number, boolean, null, undefined, symbol)
  * 2. Reference data type - memory allocate in heap - (array, object literals, dates, functions)
  */

  let name = 'kd';
  let undef = undefined;
  let nullVar = null;
  console.log('Data type is: ', (typeof name));
  console.log('Data type is: ', (typeof undef));
  console.log('Data type is: ', (typeof nullVar));

  